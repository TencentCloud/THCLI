# 依赖一律从公共 registry 安装。
#
# 显式写死而不依赖各人本机配置：package-lock.json 的 resolved 记录依赖从哪下载，
# 而外部贡献者 clone 后要能直接 npm install——本机全局配置如果是私有镜像，
# 装出来的 lock 会指向一个外部访问不到的地址，等于仓库拿下来就装不上。
registry=https://registry.npmjs.org/
